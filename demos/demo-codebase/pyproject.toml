[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "demo-codebase"
version = "0.1.0"
description = "Small intentionally vulnerable service used by the LLM Fuzz CI demo"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
