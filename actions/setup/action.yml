name: Set Up LLM Fuzz CI
description: Install LLM Fuzz CI from this action repository.

runs:
  using: composite
  steps:
    - name: Install LLM Fuzz CI
      shell: bash
      run: python -m pip install "$GITHUB_ACTION_PATH/../.."
